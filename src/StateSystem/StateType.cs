namespace StateSystem;
public enum StateType
{
    STATE_NUMBER,
    STATE_STRING,
    FLAG_SUCCESS,
    FLAG_FAILURE,
    FLAG,
    STATE_USER,
    STATE_QUESTION,
    STATE_USERNAME,
    STATE_PASSWORD,
    STATE_AUTH
}