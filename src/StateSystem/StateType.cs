namespace StateSystem;

public enum StateType
{
    STATE_NUMBER,
    STATE_STRING,

    STATE_QUESTION,

    STATE_USER,
    STATE_USERNAME,
    STATE_PASSWORD,
    STATE_AUTH,


    FLAG,
    FLAG_ACTIVE_USER,
    FLAG_SUCCESS,
    FLAG_FAILURE,

    FLAG_NONNULL,
    FLAG_NULL,

    FLAG_IDENTIFIER,
    FLAG_VALUE
}